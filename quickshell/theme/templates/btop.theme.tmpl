# Quickshell Theme
# Generated automatically — do not edit manually.

theme[main_bg]="{{background}}"
theme[main_fg]="{{text}}"

theme[title]="{{text}}"
theme[hi_fg]="{{accent}}"

theme[selected_bg]="{{surface}}"
theme[selected_fg]="{{accent}}"

theme[inactive_fg]="{{textMuted}}"

theme[graph_text]="{{textSecondary}}"
theme[meter_bg]="{{surfaceSecondary}}"

theme[proc_misc]="{{accentSecondary}}"
theme[cpu_box]="{{accent}}"
theme[mem_box]="{{accentSecondary}}"
theme[net_box]="{{info}}"
theme[proc_box]="{{accent}}"

theme[div_line]="{{surfaceSecondary}}"

theme[temp_start]="{{success}}"
theme[temp_mid]="{{warning}}"
theme[temp_end]="{{danger}}"

theme[cpu_start]="{{accent}}"
theme[cpu_mid]="{{accentSecondary}}"
theme[cpu_end]="{{info}}"

theme[free_start]="{{success}}"
theme[free_mid]="{{success}}"
theme[free_end]="{{success}}"

theme[cached_start]="{{info}}"
theme[cached_mid]="{{info}}"
theme[cached_end]="{{info}}"

theme[available_start]="{{accent}}"
theme[available_mid]="{{accent}}"
theme[available_end]="{{accent}}"

theme[used_start]="{{accentSecondary}}"
theme[used_mid]="{{accentSecondary}}"
theme[used_end]="{{accentSecondary}}"

theme[download_start]="{{info}}"
theme[download_mid]="{{info}}"
theme[download_end]="{{info}}"

theme[upload_start]="{{accentSecondary}}"
theme[upload_mid]="{{accentSecondary}}"
theme[upload_end]="{{accentSecondary}}"

theme[process_start]="{{accent}}"
theme[process_mid]="{{accentSecondary}}"
theme[process_end]="{{info}}"
